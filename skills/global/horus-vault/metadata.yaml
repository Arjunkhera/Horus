id: horus-vault
name: "Horus Vault"
version: "1.1.0"
description: "Vault MCP reference — remote knowledge base with vault-router, reader pool, stateful writer, flat-UUID storage, wiki-link auto-edges, and a 17-tool write/read/graph API."
type: skill
author: "Horus"
tags: [vault, mcp, knowledge-base, remote, graph, pages, write-path, router]
files: ["SKILL.md"]
