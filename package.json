{
  "name": "horus",
  "version": "0.1.0",
  "private": true,
  "description": "Horus — Personal Developer OS",
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r typecheck",
    "clean": "pnpm -r exec -- rm -rf dist node_modules"
  },
  "devDependencies": {}
}
