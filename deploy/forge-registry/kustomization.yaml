apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: horus-system

commonLabels:
  app.kubernetes.io/part-of: horus-control-plane
  app.kubernetes.io/component: forge

# Pinned image tag (ADR-0007 — never :latest).
# sha-f607219 = master build of PR #363 (bug b18ccd0e: DELETE /repos no longer
# 500s / orphans the Typesense index). Supersedes sha-270b977 (PR #361).
images:
  - name: ghcr.io/arjunkhera/horus/forge-registry
    newTag: sha-f607219

resources:
  - forge-registry.yaml
