id: project
name: Project
description: "Container for related work and stories"
icon: "folder"
fields:
  status:
    type: enum
    values: [active, paused, completed, archived, cancelled]
    default: active
    required: true
  priority:
    type: enum
    values: [P0-critical, P1-high, P2-medium, P3-low]
    default: P2-medium
    required: false
  goal:
    type: text
    required: false
template:
  frontmatter:
    status: active
    tags: []
  body: |
    ## Goal

    ## Scope

    ## Notes
