id: person
name: Person
description: "Contact or team member"
icon: "user"
fields:
  email:
    type: string
    pattern: "^[^@]+@[^@]+\\.[^@]+$"
    required: false
    search_mode: term
  team:
    type: string
    required: false
    search_mode: term
  role:
    type: string
    required: false
    search_mode: text
  birthday:
    type: date
    required: false
    search_mode: term
template:
  frontmatter:
    tags: []
  body: |
    ## About

    ## Notes
